# Header-only dependency for ui_events.h (enums/types only, no IPC)
ui_events_dep = declare_dependency(
  include_directories : ['inc'],
)

# Header-only dependency for ui_messaging.h
ui_messaging_dep = declare_dependency(
  include_directories : ['inc'],
)
