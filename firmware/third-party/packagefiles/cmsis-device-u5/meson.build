src_dir = '../../cmsis-device-u5'

cmsis_device_u5_includes = include_directories(
  src_dir/'Include',
)

cmsis_device_u5_dep = declare_dependency(
  include_directories: cmsis_device_u5_includes,
)
