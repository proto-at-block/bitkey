# Rust crates for the app

This directory contains Rust crates used by the mobile app. Some crates are
app-only, while others are shared with f8e (server) and wrapped by `*-ffi`
crates for mobile bindings.

See `docs/docs/mobile/architecture/rust-uniffi-bindings.md` for binding
pipelines, crate index, and regeneration steps.
