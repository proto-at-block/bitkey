# Vendored Maven Artifact - BDK 0.32.1

This directory mirrors a Maven repository layout and currently stores the official
`org.bitcoindevkit:bdk-android:0.32.1` files downloaded from Maven Central. CI and local builds
consume the artifacts directly from here, so **do not delete them**.

