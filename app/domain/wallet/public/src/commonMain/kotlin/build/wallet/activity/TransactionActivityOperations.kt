package build.wallet.activity

import build.wallet.worker.RefreshOperation

/**
 * Refresh operations associated with transaction activity
 */
val TransactionActivityOperations = RefreshOperation("Transaction Activity")
