This module contains components for implementing [LDK](https://github.com/lightningdevkit) (Lightning Development Kit) bindings.

Each platform implements bindings and injects them via `ActivityComponentImpl.kt`.
