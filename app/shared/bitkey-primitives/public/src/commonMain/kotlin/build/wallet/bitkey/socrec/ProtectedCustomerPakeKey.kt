package build.wallet.bitkey.socrec

import build.wallet.crypto.PakeKey

/**
 * Represents a PAKE key belonging to the Protected Customer, used within the SocRec verification protocol.
 */
interface ProtectedCustomerPakeKey : PakeKey
